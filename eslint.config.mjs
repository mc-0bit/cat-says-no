import unjs from 'eslint-config-unjs';

export default unjs({
    ignores: ['./src/public/linkedom-bundle.js'],
    rules: {
        // rule overrides
    },
    markdown: {
        rules: {
            // markdown rule overrides
        },
    },
});
